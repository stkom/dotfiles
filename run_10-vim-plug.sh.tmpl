{{- /* vim: set ft=sh: */ -}}
#!/usr/bin/env bash

dst="$HOME/.vim/plug.vim"

{{ if .enable_vplug -}}
if [[ ! -e $dst ]]; then
  curl -fLo "$dst" -s --create-dirs \
    'https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim'
fi
{{- end }}
