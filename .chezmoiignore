inc/

{{ if not .enable_vplug -}}
  .vim/
{{- end }}

{{ if ne .chezmoi.hostname "hitagi" -}}
  .gitconfig
{{- end }}

{{ if or (eq .chezmoi.os "freebsd") (eq .chezmoi.osRelease.id "arch") -}}
  .local/share/fzf/
{{- end }}
