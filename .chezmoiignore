inc/

{{ if not .enable_vplug -}}
  .vim/
{{- end }}

{{ if ne .chezmoi.hostname "hitagi" -}}
  .gitconfig
{{- end }}

{{ if not (hasKey .chezmoi "osRelease") -}}
  {{ set .chezmoi "osRelease" (dict "id" "") }}
{{- end }}

{{ if or (eq .chezmoi.osRelease.id "arch") (eq .chezmoi.os "freebsd") -}}
  .local/share/fzf/
{{- end }}
